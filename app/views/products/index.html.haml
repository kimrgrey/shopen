.container
  = render partial: 'products/toolbar'
  %section#latest
    - if params[:view_mode] == 'grid'
      = render partial: 'products/grid', locals: {products: @products}
    - else
      = render partial: 'products/list', locals: {products: @products}
    .pull-right
      = paginate @products